# Production docker-compose.yml — generated
version: '3.8'
services:
  frontend-frontend:
    image: ghcr.io/elmadaniabdelhamid/react-spring-frontend-0:latest
    restart: unless-stopped
    depends_on:
      - root-backend
    ports:
      - '80:80'
    environment:
      - NODE_ENV=production
      - REACT_APP_API_URL=${API_URL}
  root-backend:
    image: ghcr.io/elmadaniabdelhamid/react-spring-backend-1:latest
    restart: unless-stopped
    ports:
      - '8080:8080'
    environment:
      - SPRING_PROFILES_ACTIVE=production
      - JAVA_OPTS=-Xmx512m -Xms256m
